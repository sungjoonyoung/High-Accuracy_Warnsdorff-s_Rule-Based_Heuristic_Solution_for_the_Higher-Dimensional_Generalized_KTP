#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <algorithm>
#include <tuple>
#include <cmath>
using namespace std;

int N=100;
int arr[100][100][100][100];

int main(void) {
    int a;
    cin>>a;
    cout<<"A";
    for(int i=0;i<N;i++)
        for(int a=0;a<N;a++)
            for(int b=0;b<N;b++)
                for(int c=0;c<N;c++)
                    arr[i][a][b][c]=1'000'000'000;
    
    cout<<"A";
}
